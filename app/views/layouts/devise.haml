!!!
%html{ lang: 'en' }
  = render 'layouts/head'

  %body
    = render 'layouts/header'
    .container
      .row
        .col-lg-12
          = render 'layouts/messages'
          = form_errors_for resource
          = yield
